---
interface Props {
  type: 'success' | 'warning' | 'info' | 'danger';
  title?: string;
}

const { type, title } = Astro.props;

const typeStyles = {
  success: 'bg-success/10 border-success',
  warning: 'bg-warning/10 border-warning',
  info: 'bg-info/10 border-info',
  danger: 'bg-danger/10 border-danger',
};

const icons = {
  success: '✅',
  warning: '⚠️',
  info: '💡',
  danger: '❌',
};
---

<div class={`callout p-6 rounded-lg border-l-4 my-6 ${typeStyles[type]}`}>
  {title && (
    <div class="callout-title font-semibold mb-2 flex items-center gap-2">
      <span>{icons[type]}</span>
      <span>{title}</span>
    </div>
  )}
  <div class="callout-content">
    <slot />
  </div>
</div>
